---
import TreatmentOverviewCards from "./TreatmentOverviewCards.astro";

interface Props {
  currentHref: string;
}

const { currentHref } = Astro.props;
---

<section class="content-section treatment-nav-section">
  <div class="container">
    <div class="home-section-heading narrow-heading treatment-nav-heading">
      <h2>施術案内</h2>
    </div>

    <TreatmentOverviewCards currentHref={currentHref} />
  </div>
</section>
