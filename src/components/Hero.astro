---
interface Props {
  title: string;
  intro?: string;
}

const { title, intro } = Astro.props;
---

<section class="hero">
  <div class="container">
    <h1>{title}</h1>
    {intro && <p class="hero-intro">{intro}</p>}
  </div>
</section>
